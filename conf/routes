# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index
GET     /login/?                                Authentication.loginList
*       /login/{methodName}                     Authentication.loginMethod
*       /login/{methodName}/return              Authentication.loginReturn
GET     /logout                                 Authentication.logout
*       /register                               Authentication.register
*       /forgot                                 Authentication.recover
GET     /profile                                Authentication.profile
*       /profile/update                         Authentication.updateProfile
*       /profile/password                       Authentication.updatePassword


*       /submit                                 Submit.verifyPersonalInformation
*       /submit/verifyinfo                      Submit.verifyPersonalInformation
GET     /submit/verifyinfo                      Submit.verifyPersonalInformation
POST    /submit/doVerifyinfo                    Submit.doVerifyPersonalInformation
GET	    /submit/license                         Submit.license
POST    /submit/doLicense                       Submit.doLicense
GET   	/submit/docinfo                         Submit.docInfo
POST    /submit/docinfo                         Submit.docInfo
GET	    /submit/upload                          Submit.fileUpload
POST    /submit/upload                          Submit.fileUpload
GET	    /submit/confirm                         Submit.confirmAndSubmit
POST    /submit/confirm                         Submit.confirmAndSubmit
POST    /submit/review                          Submit.review
GET     /vireoAdmin                             Application.vireoAdmin
GET     /settings                               Application.settings
POST    /settings/vireo/admin/settings          Application.postSettings

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
#*       /{controller}/{action}                 {controller}.{action}
